# Specify the IP addresses of each node, followed by its port.
# The first address corresponds to the master node. A worker can also be run on the same machine, but with a different port.
127.0.0.1:11110
127.0.0.1:11115